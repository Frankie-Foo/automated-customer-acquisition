# 客户触达批次发布前审查

日期：2026-09-22。比较基线：c5e02d4。范围：本次批次台账、草稿归因、前端展示及只读预览；不代表500人背调、群发或生产部署已完成。

## 已修复

1. 后台生成草稿时未采用解析后的销售身份，销售视图可能漏计自己的草稿。
2. 审核后的草稿在批次面板仍显示未审核；现在读取真实审核状态，保留发送失败、回复等投递状态。
3. 从客户池重新打开草稿会丢失批次，后续审核可能记入更新的名单；现在从草稿保存的归属恢复。
4. 只读预览允许访问会同步发件配置的GET接口；现在在进入业务处理前拒绝。
5. 只读预览拒绝退出登录；现在沿用正常退出处理与来源校验，撤销会话、清除Cookie。

## 验证

- 全量pytest：536 passed，无跳过。可选PostgreSQL测试本次启用；批次测试仅使用会话临时表并回滚，其他已有SQL测试只读或使用临时表，不写生产业务数据。
- 批次专项：66 passed，覆盖销售隔离、重复登记、批次归属、原始记录、历史回复排除、草稿恢复及审核状态。
- 预览接口专项：20 passed，全部模拟数据，覆盖退出、登录、改密权限、跨站请求拒绝及写接口拦截。
- 前端npm run check：4个测试和Vite构建通过；build:python-static通过。
- Playwright：分页、搜索、正文转义、1440/390像素布局、重新打开草稿后审核保留原批次，全部通过。网络接口使用合成数据，无真实发送。
- Python wheel构建通过；批次服务和HTML引用的静态资源存在，未包含本地config.yaml、.env或outputs。
- git diff --check通过。

## 边界

本轮未部署生产、未启动获客或发信任务、未使用第三方富化额度，未跑本轮Docker部署验收。新的批次功能没有数据库迁移。

提交不包含本地配置、凭据、客户Excel或真实背调JSON。完整批量背调、邮件资产检查及真实投递属于后续业务执行，不可用本次工程测试结果替代。

## 生产执行前补查

- 7182217 已构建并滚动替换 Web、收件、获客、ContactOut worker；健康检查及公网 live 通过。数据库已备份，旧容器保留；迁移两次均 applied=[]。
- 构建时 Docker Hub TLS 超时；依赖文件未变，改为复用现有生产基础镜像并离线安装受测 wheel，没有把运行凭据加入构建上下文。
- 发送前发现手动发送依赖的共享 readiness 未拦截已退订、退信、投诉、blocked 及 ICP disqualified。已在共享入口修复；不把未评估或 review 状态误当作拒绝。
- 定向测试 112 passed。全量第一次 580 passed、1 项本地 HTTP 测试遇 Windows 10053 连接中断；该项单独复跑通过，随后全量重跑 581 passed、无跳过。wheel 重建通过。
- 以上新增拦截补丁须部署后才能发送本次业务邮件。测试和草稿 MIME 构造不算真实发送；实际发送数量以生产批次记录为准。

## 生产完成记录

- 应用代码 c3b3afb 已推送，生产镜像 salesbot:c3b3afbcaf76；Web 和三个 worker 均 healthy，公网 live=200。发布前数据库备份已验证目录，备份文件权限 600、目录 700，旧容器保留供回滚。
- 补丁上线后实际调用共享发送门禁验证四种抑制状态均被拒绝；严格 readiness 通过，重复迁移均 applied=[]。
- April India 500 批次实际归属 499 人，另 1 位仍保留原销售归属。本次 20 人完成公开商业背调和来源留存，7 人通过内容及联系人检查后发送，13 人暂缓并留有原因；余下 479 人尚未背调或发送，未启动无审核群发。
- 7 封均通过正常草稿审核、发送 API 和配额流程，发件人为 April；数据库有真实 SMTP Message-ID、主题、最终正文、收件人、背调及附件清单。7 条对应的 open followup task 均存在，预计三天后跟进。发送扣减 7 次，April 当日剩余 73/80。
- 每封保留品牌 PDF、名片图和 5 张产品图；用真实 SMTP MIME 构造器的内存接收端核验 CID 完整性、PDF 类型、署名及大小后才发送。SMTP 接收不等于客户收件箱送达，后续退信与回复继续按真实事件记录；本轮截止时无回复、无发送错误。
- 公网生产 UI 经管理员临时会话实测：批次 499/背调 20/发送 7，搜索指定客户、展开正文显示 April 发件地址，1440/390 视口截图、无页面级横向溢出、无 JS 异常。所有测试会话均撤销。April 网页仍要求本人首次改密，本次未重置或跳过该要求；以 April 身份的真实 API 批次视图及发送流程已验证。
- 前端截图与业务明细留在本地忽略目录 outputs，未向 Git 提交客户资料、密码或会话令牌。
