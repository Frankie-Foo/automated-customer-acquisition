# 2026-09-07 发布验收

## 版本

- PR：[#3](https://github.com/Frankie-Foo/automated-customer-acquisition/pull/3)，已合并。
- 主分支合并提交：`eb9660f3f75ba9a637617c694bb7f569e5af6ea5`。
- 应用提交：`dc407ae95f36dc870d3fb37074a640dc542c28fc`，与合并提交文件树相同。
- 发布镜像：`salesbot:dc407ae95f36`。
- 发布目标：`https://global-autoleads.vertu.cn`。

## 本次变化

- 已发送邮件增加按发送周汇总的效果面板，支持 7/14/30 天观察期。
- 回复按邮件 Message-ID 精确归因；重复、观察期外和无法归因的回复不参与话术胜出判断。
- 样本和差异不足时不选优胜话术；选中后保留约 20% 对照流量，证据不足可撤销。
- 修复已发送视图自动跳回和退出登录时跟进请求偶发 500。
- PWA 提示更新，不强制刷新正在编辑的页面。
- 更新受影响的前端构建依赖，npm audit 为 0 项已知漏洞。

## 发布前验证

- 本地 PostgreSQL 完整测试：425 passed。
- GitHub 主分支 CI：423 passed、2 skipped；跳过项为需显式指定隔离数据库的测试，已在本地真实 PostgreSQL 执行通过。
- 前端构建、2 项 PWA 测试通过。
- 本地三个账号真实登录、权限隔离、退出、刷新保持视图、桌面/手机布局通过。
- 隔离 Docker 完成 49 个迁移；重复 migrate 返回空列表；容器 healthy。
- 最终镜像的本地管理员 API、7/14/30 天接口、匿名拒绝、非法参数、静态资源、PWA 通过。
- Python 包包含网页和邮件图片，未包含 .env。
- PR CI：[通过](https://github.com/Frankie-Foo/automated-customer-acquisition/actions/runs/34070161711)。
- 主分支 CI：[通过](https://github.com/Frankie-Foo/automated-customer-acquisition/actions/runs/34070337311)。

## 生产发布

2026-09-07 08:54（北京时间）发布完成，服务与只读验收通过。

- 网页、邮箱轮询、获客调度、ContactOut worker 均使用 `salesbot:dc407ae95f36`，状态 running/healthy。
- 镜像在两台 Docker 引擎上的 RootFS 层一致；配置语义一致。镜像 ID 因配置 JSON 序列化顺序不同而不同。
- 严格配置检查 `ready=true`；生产重复迁移返回 `applied=[]`，共 49 个版本。
- 公网 `/api/live=200`，新 JS/PWA 可读取，匿名邮件效果接口返回 401。
- 公网桌面/手机版登录页无溢出、无浏览器脚本错误或 HTTP 5xx；未测试生产密码登录。
- 生产只读 RLS：1 个管理员、2 个销售的 7/14/30 天统计与独立 SQL 一致，未创建登录会话。
- 发布切换前后：contacts 605、outreach_messages 222、interactions 92、sales_users 20，均一致。
  这些是表记录数，不是本月新增或全量历史发信统计。
- 后台首轮 `scheduler.completed` 无 errors，enriched=0、queued=0、sent=0；ContactOut processed=0。
- 飞轮执行成功；学习状态 collecting，4 个校准样本不足，applied=[]，没有强行调整阈值或选出优胜话术。

- GitHub 自动部署仍排队；已取消本次排队任务，避免与手动部署重叠。
- 使用现有授权 Docker 连接，传输并复用本地验证的镜像，不在服务器重新解析依赖。
- 迁移前生产版本已为 49；本次没有新增 SQL 迁移。
- 生产数据库备份：`/var/lib/postgresql/salesbot-release-backups/salesbot-dc407ae95f36-1788741839.dump`。
- 备份大小：2,546,742 字节；`pg_restore --list` 校验通过。此次没有执行整库恢复演练。
- 原容器保留为 `salesbot-*-rollback-dc407ae95f36`，原环境变量和配置文件保持不变。
- 不修改密码，不启用新的自动发信或付费富化，不访问飞书。

## 验收边界

- 08:54 首次发布时，ContactOut 自动队列返回 `bridge_unconfigured`，Apollo 自动队列返回 `apollo_unconfigured`。用户随后授权复用现有配置，09:24 已完成下文的容器配置同步。ContactOut 内部连接通过，但该轮无候选，不将其计作真实查询验收。
- 环境中的管理员初始密码与现有账号不一致，未重置；本次不将其计作生产密码登录验收。
- 本地登录验收不等于真实客户效果验证；此版本不宣称回复率或成交率已经提高。
- 周汇总按当前客户负责人统计，不是冻结的历史销售绩效账本，也不是因果实验结论。
- 后台容器现有健康检查依赖网页存活，不足以证明每次邮箱/API 业务执行成功，业务结果需看任务记录。

## 现有渠道配置复用

2026-09-07 09:24（北京时间）完成，仅修改生产容器环境变量，应用镜像不变。

- 根因是容器配置不一致，不是用户没有提供密钥：独立 ContactOut worker 已有 Bridge 配置，网页已有 Apollo 配置，获客调度容器未同步。
- 从 `salesbot-contactout-worker` 复用 `CONTACTOUT_BRIDGE_URL`、`CONTACTOUT_BRIDGE_KEY` 到网页和获客调度容器。
- 从 `salesbot-web` 复用 `APOLLO_API_KEY`、`APOLLO_PHONE_ENABLED`、`APOLLO_GLOBAL_DAILY_CREDIT_LIMIT`、`APOLLO_WEBHOOK_SECRET` 到获客调度容器。
- 仅重建网页和获客调度容器，保留原镜像、启动命令、网络、配置文件和其他环境变量；邮箱轮询、独立 ContactOut worker、Bridge、数据库均未替换。
- ContactOut 全局日限额仍为 50，Apollo 全局日限额仍为 18 积分、单次预占上限 9 积分。数据库中每个销售和账号的额度前后完全一致，未提高额度。
- 密钥仅在内存和 Docker 容器配置中复用，没有输出密钥或提交 `.env`，没有访问飞书。

### 生效验证

- 网页、获客调度的实际运行配置：`contactout_configured=true`、`apollo_configured=true`。
- 两个容器的 Bridge `/ready` 通过；带正确内部凭据的空请求返回 `400 invalid_request`，在调用 Provider 前被拒绝，用于验证内部连接和认证，不查询人员或消耗额度。
- 四个应用容器均 running/healthy，网页严格配置检查通过；公网 `/api/live=200`。
- 公网桌面/手机登录页、JS/PWA 静态资源、匿名 API 401 通过；3 个实际数据库身份的只读权限与统计检查通过。未重置密码或创建登录会话。
- 首轮真实定时调度于 `2026-09-07T01:24:46Z` 完成，`errors=[]`、`sent=0`。两个渠道均不再返回缺配置原因。

### 恢复后的实际队列结果

这次没有人工提交付费测试查询。恢复调度后，原有自动队列会按原规则执行，不能将“配置验证不收费”误解为“自动调度不产生费用”。

- ContactOut：本轮候选 0、入队 0；未重试历史失败任务。
- Apollo：本轮候选 10，入队 9；其中 2 条成功并返回 2 条电话候选、2 条无匹配、5 条被 `daily_quota_exhausted` 拦截。
- 本轮成功任务的系统台账记录共 16 积分，当日全局预占 0、已用 16，未超过 18 积分上限。此为本系统记录，未核对 Apollo 账单；电话候选不等于已验证能接通。
- 未提高销售预算、批量重置任务或主动发送邮件。

### 回滚与后续发布

- 本次原容器保留为 `salesbot-web-rollback-config-20260907-012101` 和 `salesbot-acquisition-worker-rollback-config-20260907-012101`，原配置未变。
- 本次配置已写入新容器的启动环境，普通容器重启不会丢失。再次重建时必须沿用这些环境变量，不能退回原缺项配置。
- 当前生产为独立 Docker 容器，没有 Compose 项目标签。本次未同步未知位置的服务器 `production.env`；未来切换 Compose/CI 部署时，需要将上述配置纳入其密钥来源，并重新验证 Bridge 地址与网络模式。当前 `127.0.0.1:8790` 依赖 host 网络，不能直接套用到普通 Compose 网络。
