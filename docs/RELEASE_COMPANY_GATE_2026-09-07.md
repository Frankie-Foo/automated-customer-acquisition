# 公司资料质量门禁生产发布

2026-09-07，已部署。应用基线 `174d51ea33c10452a936c965287f8e30c92a3068`，镜像 `salesbot:174d51ea33c1`。

## 范围

- 公司名和官网均缺失，或公司名命中疑似平台占位值时，要求核对，不允许旧高分绕过联系方式富化、草稿生成和发送校验。
- 客户列表增加公司核对原因及操作入口；未新增公司资料编辑表单。没有编辑入口时仍须管理员更正。
- 没有删除客户、改派客户、增加配额或主动提交付费查询及测试发送。

## 验证

- 全量 pytest：447 passed，含隔离真实 PostgreSQL 筛选测试。
- 前端 check/build、2 项 PWA 测试通过；模拟客户的桌面/手机浏览器检查通过。
- 本地 Docker 构建及包内策略检查通过；全新隔离 PostgreSQL 49 项迁移，重复迁移 applied=[]，应用 healthy。
- 服务器构建遇 Docker Hub TLS 超时，改传本地已验证镜像。传输完成后，本地/远端全部 RootFS 层和 Config 对比一致。
- 生产四个应用容器 running/healthy，严格配置检查通过，迁移仍为 49 项。
- 公网 /api/live=200，匿名报表 API=401，桌面/手机登录页及 JS/PWA 静态资源通过；当前 JS 为 index-vHeEh3Rr.js。
- 生产只读校验共 606 条客户，75 条命中公司资料门禁；这些记录均被发送准备检查拒绝。管理员及两位销售的五种列表筛选检查通过，没有跨私池泄漏；可发送及自动补全队列排除公司异常。
- ContactOut Bridge 和 Apollo 配置仍有效；本次配置验收未调用付费 Provider。恢复后台后，原队列仍会按既有规则和额度运行。
- 三个数据库身份的 7/14/30 天报表汇总校验通过。未重置密码、未创建生产登录会话，未做生产真实发送验收。

## 备份与回滚

- 备份：`/var/lib/postgresql/salesbot-release-backups/salesbot-174d51ea33c1-1788758792.dump`，2,571,119 字节，pg_restore --list 校验通过。
- 四个旧应用容器以原名称加 `-rollback-174d51ea33c1` 保留。回滚必须先停新后台，避免新旧任务同时运行；不要覆盖生产数据库来回滚这次无新增迁移的代码变更。
- 继承旧容器的环境变量、配置文件和网络，生产数据库及 Bridge 容器未替换。

## 教程

仓库：`docs/生产版快速上手_2026-09-07.html`。

桌面：`C:/Users/frank/Desktop/VERTU自动化获客_销售使用教程_2026-09-07.html`。

文件一致性及桌面/手机排版检查通过；教程含操作步骤、邮件检查清单、回流跟进、复盘及当前功能边界。
